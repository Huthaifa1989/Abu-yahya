rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // السماح للجميع بالقراءة والكتابة بدون تسجيل دخول
      allow read, write: if true;
    }
  }
}